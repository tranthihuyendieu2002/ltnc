#include<iostream>
using namespace std;
int main()
{
    int so1, so2, so3;
    cin >> so1 >> so2 >> so3;
    if (so1 == so2 && so1 == so3) cout << "true";
    else cout << "false";
    return 0;
}
